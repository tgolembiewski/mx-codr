#!/usr/bin/env bash
# Claude Code UserPromptSubmit hook: prints the project rules, added as context to every prompt. Exit 0.
# Same rules in remind-skills-codex.sh, remind-skills-cursor.sh and RULES in plugins/mendix-mdl-harness.js.
cat <<'MSG'
Project rules (full text: `.claude/rules/mdl-skills.md`). 1. Start with `bash tests/orient.sh`, not by exploring by hand. 2. Before building or changing any feature invoke the Skill tool with `test-first-delivery`: write tests/verify-<feature>.test.sh, run `bash tests/gate.sh --only <feature> --boot-if-needed`, watch it FAIL first, then iterate on that ONE script. 3. Before creating a module or changing a Marketplace module (`<Module>Ext`): `module-structure`; before a page: `spacing-and-layout` (side-by-side widgets need `DesignProperties` Spacing, never CSS); before a microflow: `naming-and-captions` (a business `@caption` on every decision AND action). If the Skill tool does not list them, read exactly those four files, `.claude/skills/<name>/SKILL.md`, one file per command (never one combined `cat`), and nothing else up front. 4. Syntax: `./mxcli syntax <topic>`, then `./mxcli check <script>.mdl -p <app>.mpr --references` before every exec (a hook runs `tests/precheck.sh` for you -- mx check on a copy; do not call it by hand); do not sweep SKILL.md files. 5. Done = `bash tests/gate.sh` (suite + mx check + lint + coverage + naming + layout + security) ends in `DONE`.
MSG
